Слайдер:
1- в админке создаём новую запись: 
	-копируем заголовок первого слайда в файле: home.php и вставляем в запись, так же перносим текст
	-здесь же добавляем новую рубрику: слайдер и выбираем её для записи
	-устанавливаем изображение записи (если такого раздела нет, то запись сначала нужно опубликовать и он появится)
	- нажимаем: Обновить(Опубликовать) и так для всех записей слайдера
2- в файле: home.php в секции слайдера <section class="carousel"> оставим только код одного слайда, остальные удалим Его поместим в цикл который будет обрабатывать все записи и выводить на экран:
	- на сайте: https://wp-kama.ru/ -> переходим в WordPress -> Шпаргалка -> раздел Цикл The Loop (внизу примеры) -> копируем пример №4 и вставляем в файле: home.php в секции слайдера <section class="carousel"> -> оптимизируем его и помещаем туда код нашего слайда -> вместо заголовка слайда прописываем функцию: <?php the_title(); ?>, тоже для вывода текста: <?php the_content(); ?>, для вывода картинок слайда используем функцию: <?php the_post_thumbnail(); ?>

Контакты (1:55:00):
1- в админке в разделе Плагины добавим новый: Contact Form 7 (установить и активировать)
2- в меню появляется пункт: Contact Form 7 -> Контактная форма 1 -> меняем назание на: Контактная форма
3- удаляем в содержимое формы, указанное по умолчанию -> и заполняем своими:
	- для 1-го input: нажимаем вкладку: текст -> Атрибут class: contacts__input -> поле: не обязательное -> Значение по умолчанию:Как Вас зовут -> Используйте этот текст в качестве заполнителя поля -> нажать: Вставить тег
	- для 2-го input: нажимаем вкладку: телефон -> Атрибут class: contacts__input -> поле: обязательное -> Значение по умолчанию:Ваш номер телефона -> Используйте этот текст в качестве заполнителя поля -> нажать: Вставить тег
	- для button -> нажимаем вкладку: отправить -> Атрибут class: contacts__btn button -> нажать: Вставить тег и дописать самим через пробел "Отправить заявку" -> кнопка: Сохранить (внизу) -> Скопируйте этот шорткод и вставьте его в страницы в файле: home.php в секции Контакты в <form class="contacts__form"> 
		в таком виде: <?php echo do_shortcode( '[contact-form-7 id="24" title="Контактная форма"]' ); ?> 
			вместо:
				<input class="contacts__input" type="text" placeholder="Как Вас зовут?">
				<input class="contacts__input" type="tel" placeholder="Ваш номер телефона">
				<button class="contacts__btn button" type="submit">Отправить заявку</button> 

Логотип (2:03:21):
1- Т.к. логотип у нас в формате svg (запрещён для загрузки), из шпаргалки на сайте: https://wp-kama.ru/ скопируем в разделе: Как в WordPress загрузить SVG 2-а код (используем хук-фильтр):

		Шаг 1 - Включение SVG в список разрешенных для загрузки файлов:

add_filter( 'upload_mimes', 'svg_upload_allow' );

# Добавляет SVG в список разрешенных для загрузки файлов.
function svg_upload_allow( $mimes ) {
	$mimes['svg']  = 'image/svg+xml';

	return $mimes;
}

Шаг 2 - Подмена mime типа SVG:

add_filter( 'wp_check_filetype_and_ext', 'fix_svg_mime_type', 10, 5 );

# Исправление MIME типа для SVG файлов.
function fix_svg_mime_type( $data, $file, $filename, $mimes, $real_mime = '' ){

	// WP 5.1 +
	if( version_compare( $GLOBALS['wp_version'], '5.1.0', '>=' ) )
		$dosvg = in_array( $real_mime, [ 'image/svg', 'image/svg+xml' ] );
	else
		$dosvg = ( '.svg' === strtolower( substr($filename, -4) ) );

	// mime тип был обнулен, поправим его
	// а также проверим право пользователя
	if( $dosvg ){

		// разрешим
		if( current_user_can('manage_options') ){

			$data['ext']  = 'svg';
			$data['type'] = 'image/svg+xml';
		}
		// запретим
		else {
			$data['ext'] = $type_and_ext['type'] = false;
		}

	}

	return $data;
}

и вставляем в function.php

2- Теперь в настройках темы выбираем загрузить логотип (можно в svg-формате) -> Опубликовать

3- в файле header.php вместо: 
	<a class="logo" href="#">
		<img class="logo__img" src="<?php bloginfo('template_url'); ?>/assets/images/logo.svg" alt="logo">
	</a>
	пишем: <?php the_custom_logo(); ?>
Тоже для footer.php

Телефон (2:11:29):
1- в админке установим новый плагин: Advanced Custom Fields (зелёный- 2млн. установок) -> в меню появится пункт: Группы полей -> Добавить поле (2:13:10) и заполняем форму (тип поля указваем: текст (что бы можно было вводит знак +)) -> Опубликовать
2- убираем номера телефонов со страниц: header.php и home.php вместо них пишем: <?php the_field( 'phone' ); ?>
3- номер телефона теперь можно вводить при редактировании страницы home.php (как мы указали в форме выше) в разделе Группа полей (название тоже прописали в форме выше)
 Добавим ещё одно поле для ввода телефона в теге (2:19:24) где телефон будет указан без пробелов и знака + и скобок (тип поля: число)  т.е.
 в header.php получим:
<a class="phone" href="tel:<?php the_field('phone-number'); ?>"><?php the_field('phone'); ?></a>
в home.php: 
<a href="tel:<?php the_field('phone-number'); ?>"><?php the_field('phone'); ?></a>



